# Core Discord library (v2.x brings Intents & slash-command support)
discord.py>=2.3.0

# Load .env files (tokens, API keys, channel IDs, etc.)
python-dotenv>=1.0.0

# Async HTTP client used by Discord and any webhook helpers
aiohttp>=3.9.0

# Source-Engine server query (A2S) for player counts and server info
a2s>=1.0.1

# Simple synchronous HTTP for quick integrations and helpers
requests>=2.31.0

# Optional web framework support used by helper scripts
Flask>=3.0.0

# Development / testing helpers
pytest>=8.0.0
pytest-asyncio>=0.23.0